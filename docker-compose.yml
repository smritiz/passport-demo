version: '3'
services:
    passport-app:
        build: .
        depends_on:
            - mongodbB
        ports:
            - '3003:3003'
        mongodbB:
            image: mongo
        container_name: mongodb
